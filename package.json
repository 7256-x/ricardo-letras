
{
  "name": "ricardo-letras-site",
  "version": "1.0.0",
  "scripts": {
    "prebuild": "node generate-sitemap.js",
    "start": "vercel dev",
    "build": "echo 'No build step required'",
    "deploy": "vercel --prod"
  },
  "dependencies": {},
  "devDependencies": {}
}
